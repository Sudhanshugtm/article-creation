{
  "name": "article-creation",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "verify": "bash tools/verify-build.sh",
    "deploy": "npm run build && bash tools/deploy-gh-pages.sh"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.11"
  },
  "dependencies": {
    "@wikimedia/codex": "^2.2.0",
    "@wikimedia/codex-design-tokens": "^2.2.0",
    "@wikimedia/codex-icons": "^2.2.0"
  }
}
